#!/bin/bash

function log {
    echo "> ${@}"
}

log "updating dependencies"
go get -d .

log "building"
mkdir -p dist
go build -o dist/golpje *.go

log "starting"
echo '---------------------------------'
echo
dist/golpje ${@}
